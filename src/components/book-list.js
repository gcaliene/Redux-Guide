import React, { Component } from 'react';

//A container is just a component that has access to the state via Redux
export default class BookList extends Component {
  render() {
    return <ul className="list-group col-sm-4">li</ul>;
  }
}
